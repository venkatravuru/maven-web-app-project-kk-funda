apiVersion: v1
kind: ReplicationController
metadata:
  name: javawebpprc
  namespace: test
  labels:
    name: rcprod
spec:
  replicas: 10
  selector:
    app: javawebapp
  template:
    metadata:
      name: javawebapppod
      labels:
        app: javawebapp
    spec:
      containers:
      - name: javawebconn
        image: kkeducationb2/java-webapp:1.1
        ports:
        - containerPort: 8080
